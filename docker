sudo yum update -y
sudo yum install docker -y
sudo systemctl start docker
sudo systemctl enable docker #if server reboot need to enable the docker
sudo usermod -aG docker ec2-user
sudo usermod -aG docker jenkin
#docker ps
#docker images
reboot
#ones you configured sudo for docker,you need to restart ec2 instance 
